Install

sudo make install

Simply run "kaps2regions.sh" in a folder with kaps, and they will be sorted and placed into their region in a directory named "output"  Requires imgkap to read the kap header.
